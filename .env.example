# ==========================================
# 授权服务器配置 (Server Configuration)
# ==========================================

# 管理员登录密码（必须设置）
# 用于访问 /admin 管理后台
ADMIN_PASSWORD=your_secure_password_here

# 管理员 API 密钥（必须设置）
# 用于管理接口鉴权
ADMIN_API_KEY=your_admin_api_key_here

# Flask 密钥（必须设置）
# 用于 Session 加密，建议生成随机复杂字符串
SECRET_KEY=complex-secret-key-change-this-in-prod

# 数据库地址（默认使用本地 SQLite）
# 如果需要使用 PostgreSQL，请修改为: postgresql://user:password@localhost/dbname
# DATABASE_URL=sqlite:///auth.db

# 授权服务器监听地址和端口
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5005

# 授权服务器公钥 (可选，如果需要手动指定)
# LICENSE_PUBLIC_KEY=...

# 授权服务器私钥 (可选，如果需要手动指定)
# LICENSE_PRIVATE_KEY=...

# Session Cookie 安全设置 (生产环境建议开启)
# SESSION_COOKIE_SECURE=true

# ==========================================
# 客户端配置 (Client Configuration)
# ==========================================

# 授权服务器地址 (客户端连接用)
# 指向上面配置的授权服务器地址
LICENSE_SERVER_URL=http://localhost:5005

# 客户端 Web 服务监听地址 (用于远程控制/Webhooks)
# WEB_SERVER_HOST=0.0.0.0
# WEB_SERVER_PORT=5000

# 客户端公网 IP (用于生成远程控制链接)
# 如果部署在云服务器，请设置为服务器公网 IP
# WEB_SERVER_HOST_IP=localhost

# 单实例锁端口 (防止程序重复启动)
# SINGLE_INSTANCE_PORT=12345

# Playwright 浏览器路径 (可选)
# 如果需要指定特定的浏览器路径
# PLAYWRIGHT_BROWSERS_PATH=./playwright-browsers
