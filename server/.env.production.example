# 生产环境授权服务器配置
# 使用方法：将此文件重命名为 .env 并放在 server/ 目录下

# 1. 网络配置
# 监听所有 IP，允许外网访问
FLASK_HOST=0.0.0.0
# 服务端口 (建议使用 Nginx 反向代理到此端口)
FLASK_PORT=5005

# 2. 安全配置 (必须修改!)
# 管理员密码
ADMIN_PASSWORD=ComplexPasswordHere123
# 管理员 API 密钥 (用于管理接口鉴权，与客户端无关，仅用于管理员工具)
ADMIN_API_KEY=uuid-or-random-string

# Flask 会话加密密钥 (生成一个随机的长字符串)
SECRET_KEY=random-secret-key-here

# 3. 数据库配置 (可选)
# 默认使用 SQLite (auth.db)
# 如果并发高，建议使用 PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost/dbname

# 4. HTTPS/SSL (建议)
# 如果在生产环境，建议通过 Nginx 反向代理配置 HTTPS
# 并设置:
# SESSION_COOKIE_SECURE=true
